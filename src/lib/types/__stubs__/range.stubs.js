const matchResult = ['is the sample'];
Object.assign(matchResult, { index: 5, input: 'this is the sample text' });

export default {
  token: 'is the sample',
  text: 'this is the sample text',
  matchResult
};
