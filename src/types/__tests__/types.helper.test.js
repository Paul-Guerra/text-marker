/* global describe, it, expect */
// import block, { makeBlockRegex } from '../block';
// import stubs from '../__stubs__/block.stubs';

describe('setAttributes()', () => {
  it('sets an attributes property', () => {});
});
